// This component is no longer needed since we now only support one account per user
// Keeping it for backwards compatibility but it doesn't render anything
export default function AccountSwitcher() {
  // Just return null to render nothing
  return null;
}
